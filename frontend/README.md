# Funeral Homes UI (Next.js + MUI + Tailwind) 

## Setup
- Install dependencies:
```bash
npm install
```  

- Verify API base URL in `.env.local`:

```bash
NEXT_PUBLIC_API_BASE_URL=http://localhost:8081
```
 

- Run:
```bash
npm run dev
```  

Open http://localhost:3000